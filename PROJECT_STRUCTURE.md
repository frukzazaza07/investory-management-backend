
investory-management-backend/
├── docs/                              # Auto-generated by swag init
│   ├── docs.go
│   ├── swagger.json
│   └── swagger.yaml
├── internal/                          # Private application code
│   ├── database/
│   │   ├── migrate.go                 # GORM auto-migration runner
│   │   ├── postgres.go                # PostgreSQL connection via GORM
│   │   └── seed.go                    # Database seeder (initial data)
│   ├── handler/
│   │   ├── auth_handler.go            # Register, login
│   │   ├── inventory_handler.go       # Inventory item CRUD + stock adjustment
│   │   ├── pos_handler.go             # POS sale processing
│   │   ├── product_handler.go         # Product CRUD + BOM management + barcode lookup
│   │   ├── purchase_order_handler.go  # Purchase order lifecycle
│   │   ├── supplier_handler.go        # Supplier CRUD
│   │   └── webhook_handler.go         # Webhook registration + delivery
│   ├── middleware/
│   │   ├── api_key_middleware.go      # API key auth (POS routes)
│   │   └── jwt_middleware.go          # JWT validation (dashboard routes)
│   ├── models/
│   │   ├── base_model.go              # Shared GORM base (ID, timestamps, soft-delete)
│   │   ├── inventory_item_model.go    # InventoryItem (warehouse stock record)
│   │   ├── pos_sale_log_model.go      # PosSaleLog (idempotent sale record)
│   │   ├── product_model.go           # Product (barcode, SKU, POS ID) + BOM associations
│   │   ├── purchase_order_model.go    # PurchaseOrder + PurchaseOrderItem
│   │   ├── stock_transaction_model.go # StockTransaction (audit ledger)
│   │   ├── supplier_model.go          # Supplier
│   │   ├── user_model.go              # User (bcrypt password, role)
│   │   └── webhook_model.go           # WebhookSubscription + WebhookDelivery
│   ├── repository/
│   │   ├── inventory_repository.go
│   │   ├── pos_sale_log_repository.go
│   │   ├── product_repository.go
│   │   ├── purchase_order_repository.go
│   │   ├── stock_transaction_repository.go
│   │   ├── supplier_repository.go
│   │   ├── user_repository.go
│   │   └── webhook_repository.go
│   ├── router/
│   │   └── router.go                  # Route registration (JWT + API-key groups)
│   └── service/
│       ├── auth_service.go            # Password hashing, JWT sign/verify
│       ├── inventory_service.go       # Stock level logic
│       ├── pos_service.go             # Idempotent sale processing, deduction
│       ├── product_service.go         # Product + BOM business logic
│       ├── purchase_order_service.go  # PO creation, receiving, status transitions
│       ├── stock_service.go           # Stock transaction recording
│       ├── supplier_service.go
│       └── webhook_service.go         # Fanout, delivery, retry
├── pkg/
│   └── response/
│       └── response.go                # Standard API response { status, message, data }
├── scripts/
│   └── test_endpoints.ps1             # PowerShell smoke-test script
├── .dockerignore
├── .env                               # Environment variables (not committed)
├── .env.example                       # Environment variable template
├── .gitignore
├── docker-compose.yml                 # App + PostgreSQL services
├── Dockerfile                         # Multi-stage build (builder + alpine)
├── go.mod
├── go.sum
├── INTEGRATION.md                     # POS integration guide
├── PROJECT_STRUCTURE.md
└── server.go                          # Main entrypoint
