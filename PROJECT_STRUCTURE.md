
investory-management-backend/
├── cmd/                        # (reserved) entrypoint alternatives
├── docs/                       # Auto-generated by swag init
│   ├── docs.go
│   ├── swagger.json
│   └── swagger.yaml
├── internal/                   # Private application code
│   ├── database/
│   │   └── postgres.go         # PostgreSQL connection via GORM
│   ├── handler/
│   │   └── auth_handler.go     # HTTP handlers (register, login)
│   ├── middleware/
│   │   └── jwt_middleware.go   # JWT validation middleware
│   ├── repository/
│   │   └── user_repository.go  # DB queries (User model, CRUD)
│   ├── router/
│   │   └── router.go           # Route registration
│   └── service/
│       └── auth_service.go     # Business logic (hash, JWT sign/verify)
├── pkg/                        # Reusable packages
│   └── response/
│       └── response.go         # Standard API response { status, message, data }
├── .dockerignore
├── .env                        # Environment variables (not committed)
├── .gitignore
├── docker-compose.yml          # App + PostgreSQL services
├── Dockerfile                  # Multi-stage build (builder + alpine)
├── go.mod
├── go.sum
└── server.go                   # Main entrypoint